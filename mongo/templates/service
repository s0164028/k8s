apiVersion: v1
kind: Service
metadata:
  name: mongo
  namespace: apn-stack
spec:
  type: {{ .Values.service.type }}
  selector:
    app: mongo
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 27017
