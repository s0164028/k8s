apiVersion: v1
kind: Service
metadata:
  name: lighttpd
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: lighttpd
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
  type: {{ .Values.service.type }}
